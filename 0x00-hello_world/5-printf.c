#include <stdio.h>
/* The simple c program to print a string of words
 * int main() is the function that allow the prog to output or input
 * We make sure we include  #include <stdio.h> from its library for each c program
 *
 * */
int main(void)
{
         printf("with proper grammar, but the outcome is a piece of art,"); 
	 
	 return (0);
}
