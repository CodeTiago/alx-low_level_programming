#include "main.h"
/**
 * _islower - this function prototype checks for lowercase character
 * Return: 1 if it's lowercase 0 otherwise
 * c - character in the parameter for checking
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');

}
